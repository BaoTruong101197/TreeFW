/**
 *
 * @author Bao Truong
 */

#include "core/node.hpp"

#pragma one

class IO : public Node
{
public:
    explicit IO(const FieldNode & _fieldNode = {});
private:
    // Event<>& changeEvent = 
};